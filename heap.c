#include <stdio.h>
#include <stdlib.h>

int* allocate_scalar_list(int size, int mul) {
    int* mul_ptr = malloc(size * sizeof(int));
    if (mul_ptr == NULL) {return NULL;};


}


int main(void) {

}
